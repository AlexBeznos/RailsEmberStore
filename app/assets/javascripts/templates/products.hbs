<h1>Products</h1>
<ul>
	{{#each}}
		<li>{{name}}</li>
	{{/each}}
</ul>

<button {{action 'openForm'}}>{{#if isForm}}Close form{{else}}Open form{{/if}}</button>
<br>
{{#if isForm}}
  {{render 'addProduct'}}
{{/if}}